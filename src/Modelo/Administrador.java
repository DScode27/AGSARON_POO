package Modelo;

public class Administrador extends Persona{
    public Administrador(String cc, String nombre, int edad, String correo, String usuario, String password) {
        super(cc, nombre, edad, correo, usuario, password);
    }
}
